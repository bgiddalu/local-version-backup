# Local Version Backup

Create versioned backups of files and folders locally inside VS Code.

## Features

- Versioned backup folders (V x.y)
- Source mapping (files & folders)
- Destination path configuration
- Automatic and manual backup modes
- Version branching support

## Usage

1. Open Explorer sidebar
2. Configure mapping and destination
3. Click "Backup locally"
